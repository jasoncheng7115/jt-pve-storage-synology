# 變更記錄

每一個 0.x 版都是預發行版。1.0.0 是實機測試全過的那一版。

哪些事實已在實機上驗證、哪些沒有，記錄在
[docs/TESTING_zh-TW.md](docs/TESTING_zh-TW.md)——要判斷某一版能不能信任，
那份文件比這一份有用。

## [0.1.2~beta1] - 2026-08-06

### 修正

- **發佈的 `SHA256SUMS` 無法驗證。** GitHub 會把 release 附件檔名裡的 `~` 改寫成
  `.`，所以校驗檔裡寫的是 `..._0.1.1~beta1-1_all.deb`，而實際提供下載的檔案叫
  `..._0.1.1.beta1-1_all.deb`，`sha256sum -c SHA256SUMS` 會回答「找不到檔案」。
  release 工作流程現在會先把套件改名成 GitHub 實際會提供的名稱再計算雜湊，
  然後**驗證自己產生的校驗檔**，而不是假設它對。`.deb` 本身一直是正確的——dpkg 從
  套件的 control 資料讀版本，從不看檔名——所以 0.1.0~beta1 與 0.1.1~beta1 都能正常
  安裝，只有它們的校驗檔不能用。

### 新增

- 這份變更記錄，中英文各一份。`make release-check` 現在會要求發行版本必須有對應
  的條目。

## [0.1.1~beta1] - 2026-08-06

### 新增

- **快照還原的方法名稱是 `restore_snapshot`**，在實機上確認：九個候選名稱送進一台
  DSM 7.1.1，每個都指名一個 NAS 從未發出過的 LUN 與快照 uuid，其中一個回答了
  `18990505 bad LUN UUID`——證明它存在，而且已經走到查詢 LUN 那一步。兩份公開的
  參考實作都沒有這個方法。
- 記錄錯誤碼 18990004，從 `SYNO.Core.ISCSI.Host` 的 `get` 觀察到。

### 變更

- **LUN 列表不再送型態過濾條件。** Synology 自己的 CSI driver 會送一個明確的十二種
  型態清單，而在測試機上那份清單藏了一個 Virtual Machine Manager 的虛擬磁碟，它的
  120 GiB 吃的是同一個儲存空間。一個已被驗證為不完整的過濾條件比不過濾更糟，因為
  靜默少掉物件的清單，會被「決定什麼可以刪」的程式碼讀成「全部就這些」。探索工具
  現在會跑兩次列表，並回報過濾條件藏了什麼。
- **倒回仍然拒絕。** 知道一個方法存在，不等於知道它做什麼：它的參數名稱尚未確認，
  倒回後比還原點更新的快照是否保留、LUN uuid 是否不變，也都還沒確認。

### 修正

- 用 `--no-luns` 跳過列表時，登記簿會回報成「這個帳號**無法**讀取」。跳過、被拒絕、
  成功是三種不同的結果，而把前兩者混為一談正是這個專案的規則要防止的事。
- 唯讀方法成功時被標成可疑。只有破壞性候選方法成功才值得警戒。
- 檔頭註解還在描述 `--probe-methods` 送出「不帶參數」的呼叫，而改成指名一個不存在
  的 uuid 之後那句話已經不成立。

## [0.1.0~beta1] - 2026-08-06

第一個打包版本。**不含任何 storage plugin。**

### 新增

- `pve-syno-api-probe`——以唯讀方式探索 Synology DSM 的 SAN Web API。它探索 API
  路徑與版本範圍而不是寫死，工作階段兩種載體都送（因為兩份公開參考實作對哪一種
  可用的看法不同），回送兩份參考實作都不送的防 CSRF token，而且絕不把憑證放進 URL。
- `docs/TESTING_zh-TW.md`——哪些事實已在實機驗證、哪些沒有，以及補上落差的測試計畫。
- `docs/DSM-ACCOUNT_zh-TW.md`——plugin 需要的 DSM 帳號、可以從它拿掉什麼，以及讓
  「重試被拒絕的憑證」變得危險的自動封鎖行為。
- 建置守衛：不得有節點層級的 `multipath` flush，憑證不得出現在 URL。
