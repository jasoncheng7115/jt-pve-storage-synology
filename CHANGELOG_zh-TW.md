# 變更記錄

每一個 0.x 版都是預發行版。1.0.0 是實機測試全過的那一版。

哪些事實已在實機上驗證、哪些沒有，記錄在 [docs/TESTING_zh-TW.md](docs/TESTING_zh-TW.md)——要判斷某一版能不能信任，那份文件比這一份有用。

## [0.9.0] - 2026-08-07

從 0.6.x 直接跳過來是刻意的。到這一版為止，每一個 storage 操作都已經在真實硬體上、從兩種介面各驅動過一次，涵蓋兩個機型、三個 DSM 版本，跑在一個五節點的正式叢集上——包含一次節點當機、一次路徑故障，以及一次整台 NAS 中斷。1.0.0 之前還缺的是這裡沒有的硬體：一組 Synology HA，以及一台雙控制器機箱。

### 變更

- **文件直接寫清楚每一項檢查是從哪個介面驅動的**。操作清單多了一個「**從哪裡驅動**」欄位：B 到 D 各跑過**兩次**，網頁介面一次、指令列一次；A 只有指令列，因為 GUI 沒有那個頁面。這個區分不是吹毛求疵——這個專案有五個缺陷藏在「從 shell 跑完全正常」的程式碼裡，是有人從網頁按下去才現形的。
- **「如何協助」是寫給作者看的，不是寫給讀者看的**。它原本要的是驗證紀錄的項目編號、一個 WWID 衍生規則的樣本，以及一個錯誤碼，其中一項今天已經有答案了。現在改成四件任何人都做得到的事，第一件是：在本頁沒測過的 Synology 上跑一次唯讀的探索工具。
- **狀態摘要太長，而且中文讀起來不順**。砍掉大約三分之一，量測到的數字一個都沒少。
- `登記簿` 不像台灣用語，中文文件改用 `驗證紀錄`。
- 頁面上的日期拿掉了。重點是「這件事量過」，不是它發生在哪一天。

## [0.6.26] - 2026-08-07

### 變更

- **快照上限改成守在 NAS 回報的兩個數字中「較小」的那個（R-26）**。DSM 公布 `max_snapshot_per_lun` **256** 與 `max_snapshot_per_lun_v2` **128**，而且沒說它執行哪一個——DS918+／7.4.1 與 DS925+／7.3.2 回報一模一樣的一對，所以這是系統性的。哪一個是真的，唯讀查不出來；要在同一顆 LUN 上拍到第 129 張才知道。守在較大的那個，等於讓陣列先拒絕，而那正是這道檢查存在的唯一目的，所以取較小值，而拒絕訊息會把兩個數字都寫出來。

### 文件

- **頁面的段落順序和側邊欄不一致**，而且「系統需求」與「DSM 帳號」排在「安裝」和「設定」**之後**——讀者開始之前最需要的兩件事被放在最下面。現在的順序是「概覽 → 已經量測過的 → 開始之前 → 使用 → 專案」，側邊欄與它完全一致。
- **LUN 上限就是 VM 磁碟數上限，現在直接寫出來**，不再只是暗示：它是數量不是容量、是每台 NAS 一份且整個叢集共用、會把你在 SAN Manager 裡為別的用途建的 LUN 一起算進去，而一台有系統碟加資料碟的 VM 就用掉兩個。`docs/LIMITS.md` 也補上了。
- 快照那兩個上限的狀況，寫在操作者會遇到它的地方。
- DSM 帳號那一段不再以「探測就此停止，不去猜」收尾。R-14 已解答：應用程式分頁列出二十個應用程式，沒有一個是 SAN Manager，所以沒有更細的權限可以給——而那二十項**每一項都可以**拒絕，那正是上面那些強化建議值得照做的原因。

## [0.6.25] - 2026-08-07

### 修正

- **在任何曾經刪過 LUN 的節點上，`qm move_disk` 搬回本 storage 與 `qmrestore` 都會失敗**——連續四次，每次都是全新的 WWID，而且踩在兩個發版清單項目上。一顆 LUN 被刪除後 NAS 重用它的 mapping index，節點也跟著重用同一個 sd 節點：核心在 rescan 時重讀 VPD，把 `/sys/block/<sd>/device/wwid` 更新成**新的** LUN，而 **multipathd 從來沒有重讀那條路徑**，還為那顆已刪除的 LUN 守著一個 map。`ensure_map` 於是在等一個永遠建不起來的 map。現在 `activate_volume` 會請**核心**重新探索那個裝置——移除 sd 節點、重掃工作階段、重新確認 WWID、再要一次 map。從來不是資料風險：plugin 是拒絕，不是拿那個裝置去用。

### 怎麼找到的

- **對一個已經發出去的版本重跑 A–E 操作清單**。0.6.21 到 0.6.24 都只靠 `make release-check` 就出貨；第一次重跑就找到上面那個。那次重跑現在是稽核清單上的一行，而且跑在另一個叢集上。

## [0.6.24] - 2026-08-07

### 已驗證

- **R-10 完整解答，而且那是個真的風險**。在 SAN Manager 裡啟用每日 LUN 快照排程之後，DSM 自己拍的排程快照**確實**出現在該 LUN 的 `list_snapshot` 裡。它是可以分辨的：`taken_by` 是 **`scheduler`**，而這個 plugin 的是 `jt-pve-storage-synology`。端到端驗證過——NAS 上那顆 LUN 有**兩**張快照，而 `volume_snapshot_info` 對 Proxmox VE 只報**一**張，也就是它自己的。所以 PVE 不會把使用者的排程快照當成自己的，也刪不掉它，而這件事現在是量測出來的，不是設計出來的。另一半也站得住：`assert_room_for_snapshot` 用 `all => 1`，數到的是**兩**張，而那正是「與使用者排程共用的每顆 LUN 256 張」所需要的。
- **R-14 已解答，答案來自 DSM 的介面而不是它的 API**。那個帳號的**應用程式**分頁列出二十個應用程式，而其中**沒有任何一項是 SAN Manager、儲存空間管理員或 iSCSI**——所以沒有更細的權限可以給，那個存取權是隨 `administrators` 一起來的。有用的那一半是它帶來的行動：那二十項**每一項都可以**拒絕，所以「一個專用的管理員帳號、所有應用程式全部拒絕、開 2FA、以 IP 限制」就是現存最小的組態。`DSM-ACCOUNT_zh-TW.md` 現在寫的是這個，而不是描述一個待跑的檢查。

### 新增

- **R-26，未解答**。`info type=define` 回報的快照上限是**兩個**，不是一個：`max_snapshot_per_lun` **256** 與 `max_snapshot_per_lun_v2` **128**——而 DS925+／DSM 7.3.2 回報一模一樣的一對，所以這是系統性的。plugin 讀的是前者。DSM 執行的是哪一個唯讀問不出來；那需要在同一顆 LUN 上拍到第 129 張。這件事是記錄下來，不是猜出來，而程式沒有改動。

### 修正

- 文件網站上的 A–E 測試清單表格把 **A 排在最後**。現在是 A、B、C、D、E。

## [0.6.23] - 2026-08-07

### 新增

- **兩個 bin 腳本都加上了 `--version`**。兩個都沒有。它們的版本只出現在 `--help` 的標題行，所以 `pve-syno-reap --version` 會回 `Unknown option: version` 然後吐出說明——那在出事的時候讀起來像「工具壞了」，而不是「工具舊了」，而 reaper 正是文件指定的「節點當掉之後」那一步。`release-check` 現在也會檢查任何帶 `$VERSION` 的腳本**真的有**那個選項，因為沒人讀得到的版本不算版本。已在拿掉選項的情況下示範會失敗。

### 修正

- **0.6.21 的一項更正本身是錯的**。那則說 `pve-syno-reap --version` 一直報 `0.6.5`；並沒有，因為那個選項不存在。真正過舊的是 `--help` 的標題行。偏移是真的，修法也是對的，但寫錯了是哪一個輸出——是在安裝完那個「宣稱已修好」的版本之後，在五台節點上實際去跑它才發現的。兩份變更記錄現在都改成 `--help`。**沒有跑過就斷言一個命令的輸出，就是一項更正變成第二個錯誤說法的方式。**

## [0.6.22] - 2026-08-07

### 修正

- **快照上限數的是 DSM 交出來的陣列，不是 DSM 自己回報的總數**。`list_snapshot` 會在 `snapshots` 陣列旁邊回一個 `count`，而 `snapshot_list` 從來沒讀它——所以一份短少的清單會讓 `assert_room_for_snapshot` 在其實沒有空間的地方找到空間，而在那裡少算會往「**把快照拍下去**」的方向失敗。現在兩者不一致時它會拒絕，並且說清楚問題在那個數字，而不是去怪上限。不回報 `count` 的固件不受影響，那一點有自己的測試涵蓋。

### 已驗證

- **R-9 已在 DSM 7.4.1 上以唯讀方式重新量測**，而且是分清單回答，不再當成一個問題。`LUN list` 與 `Target list` **完全忽略 `offset`／`limit`**——`limit=1` 回來的和不帶參數是同一份完整清單——而且都不回報總數。`LUN list_snapshot` 是例外：它會回報總數，而那個總數在全部九顆 LUN 上都與列數一致，包括其中一顆有快照的。參數被忽略是最強的證據，因為會分頁的 API 一定遵守 `limit`；量測時是 9 顆 LUN、4 個 target、1 張快照，而單一次量測無法排除更高數量上的門檻。這正是為什麼那個一致性是在程式裡對照，而不是假設。

## [0.6.21] - 2026-08-07

### 修正

- **`bin/pve-syno-reap` 的 `--help` 標題行有十五個版本都印著 `0.6.5`**。版本一致性檢查只點名 `bin/pve-syno-api-probe`，而這個專案自己的文件卻說它比對**兩個**帶版本號的 bin 腳本。於是守門看起來是滿足的，偏移是看不見的——而且偏偏發生在那個「出事時操作人員真的會去讀它輸出」的腳本上，因為 reaper 正是文件指定的「節點當掉之後」那一步。現在這道檢查改成列舉每一個帶 `$VERSION` 的 bin 腳本，不再點名，所以下一個新增的腳本不需要靠誰記得就會被涵蓋。修好之前已先在那個過舊的檔案上示範過它會失敗。

### 變更

- **文件網站原本寫「從快照複製要用指令列」**。不是這樣，那一段寫得比事實絕對：Proxmox VE 用 `isTemplate ? 'clone' : 'copy'` 決定要求哪一種複製，所以對**範本**它要求連結複製，而這個 storage 從快照支援它，網頁介面是可以做的——已在真實叢集上驗證。只有非範本才會被逼進那個會被拒絕的完整複製。現在兩條路都寫清楚了，而 `--full 0` 標成必要而非選用（`full` 對非範本預設為真）。
- **Btrfs 的要求寫在 `pvesm add` 命令之前**，不是塞在後面某一句的子句裡。`on_add_hook` 會去讀 `fs_type` 並拒絕 ext4 儲存空間，所以那是動手之前要先在儲存空間管理員裡確認的前提，不是事後才發現的細節。
- **「在其中一個節點上執行一次就好」改成重點**，並且和安裝那一步（每個節點都要）對照著寫。這兩個相鄰的段落先前讀起來一模一樣。
- 還是純文字的命令區塊都上了色，`docs/index.html` 裡最後幾個字面的 Markdown 反引號也改成了 `<code>`。

### 記錄

- **為什麼 `copy => { snap => 1 }` 永遠不能宣告**，而不只是「現在沒有宣告」。那個看起來可行的修法——在 `activate_volume($snapname)` 裡把快照 reflink 成一顆暫時 LUN，再在 `deactivate_volume($snapname)` 裡拆掉——不能成立，因為 PVE 從來沒有把拆除那一半配對起來：`API2/Qemu.pm` 只有在「複製到別的節點且來源已關機」時才會帶著 snapname 去 deactivate，而 `QemuServer/QemuImage.pm::convert` 只 activate，從不 deactivate。那顆暫時 LUN 會在一般的同節點複製上、在**成功的路徑上**外漏，而漏掉的 reflink 會隨著來源改動自己長大。瓶頸不是「怎麼在快照上生出可讀的裝置」，是「沒有地方把它交回去」。

## [0.6.20] - 2026-08-07

### 新增

- **`docs/og-image.png`**——分享連結時顯示的那張卡片。`netapp` 與 `purestorage` 兩個專案有，這個沒有，而那比「少一個裝飾」嚴重：`twitter:card` 早就宣告成 `summary_large_image`，而那種卡片**需要**圖片。沒有圖片它會靜默退回成純文字摘要，所以過去每一次分享出去的連結，看到的都是那個退化版。主色是 **`#0086E5`**，那是從一台真實 NAS 上讀到的 DSM 自己的 `mask-icon` 顏色，不是挑的；版面沿用相關專案的卡片，讓四支讀起來是同一個家族。
- **`make og-image`**，以及 `release-check` 裡的 **`check-og-image`**。那個徽章帶著版本號，所以檔案每次發行都會過舊；這道檢查會在它比 `debian/changelog` 舊時失敗，而不是交給誰去記得。已在刻意做舊的檔案上示範過會失敗。

### 修正

- **三個社群描述全都還寫著「discovery stage」**——那是這個專案還沒有任何硬體時的樣子。它此後已經在兩個機型、三個 DSM 版本、一個五節點的正式叢集上完整跑過。那幾個字串正是別人分享連結時會看到的東西，所以它們是這個專案裡最多人讀到的過舊文字。

## [0.6.19] - 2026-08-07

### 新增

- **發行前的操作清單終於寫下來了**。它先前只存在於一段對話裡：A–E 清單、每一列走到什麼，以及事後對兩邊的盤點——還有讓它有價值的那條規則，也就是它**必須從網頁介面驅動**。`pvedaemon`、`pveproxy`、`vzdump` 與 `pct` 在 `-T` 底下執行而且沒有 `PATH`；`qm`、`pvesm`、`pvesh`、`qmrestore` 不是。有五個缺陷藏在「從 shell 跑完全正常」的程式碼裡。**從 shell 跑一輪是回歸測試，不是驗證。**
- 那份清單的**結果**放上了文件網站，而盤點是單獨寫出來的，因為「操作回報成功」和「沒有留下任何東西」不是同一件事——而那個盤點正是「`flush_map` 從來沒有真的移除過任何 map」被抓到的方式。

### 文件

- **狀態框原本是一段跑不完的句子，而裡面有三個事實已經過舊**：一個機型、一個 DSM 版本、三個節點、二十五個缺陷。現在是四句帶標籤的話——跑過什麼、哪些機型與 DSM 版本、仍然該誠實說的是什麼——數字都是最新的。另外「60 次讀取失敗 0 次」的寫法也可能被讀成「失敗 60 次」。
- 九個協定發現與 WWID 的推導搬到驗證紀錄，成為**協定實際上長什麼樣**。那些沒有任何一項是安裝或使用這個 plugin 所必需的，而它們原本是狀態框之後頁面上的第一個東西。
- **DS925+ 進到各機型表格與「實機已驗證」一節了**。量測了第二個機型卻沒有寫在它該出現的那一頁上，是讓那次量測價值變低的疏漏。

## [0.6.18] - 2026-08-07

### 文件

- **「安裝」與「更新」變成了一面牆**。每找到一件事就往後加一個警告框，加到操作者在讀到任何可執行的東西之前先看到六個。現在它們只保留：指令、那個會**靜靜把節點降級**的陷阱（`wget` 少了 `-O`），以及維護時段那一則。其他全部——為什麼每個節點都要裝、版本混雜時的症狀、為什麼是 `apt install ./…` 而不是 `dpkg -i`、`reload-or-try-restart` 那段 journal——都搬到 `docs/TESTING.md` 新增的**給想深入的人**一節，那裡才是這個專案存放「量測到什麼」的地方。一般讀者不需要它，而那一頁快被淹沒了。
- **「倒回」那一節從考古開始講**。它一開頭在解釋這個方法是靠對一台 DSM 詢問九個候選名稱找到的，以及三個條件事先沒有一件是可知的。現在它一開頭講倒回**做什麼**，以及讓它安全的那兩個量測到的事實；發現過程放進驗證紀錄。
- **兩個框放錯章節**：「從快照複製」在「安裝」底下，而「第一次安裝請排維護時段」在「更新」底下。
- **開發路線圖過舊**。它寫著 `1.0.0` 在等第二個機型與第二個 DSM 版本；兩者都已完成，所以它現在寫的是真正還剩下的東西。`R-15` 標記為已縮小，附上 7.1.1 → 7.4.1 的量測，並且明確寫出「整台 NAS 重開機不是故障切換，沒有回答它」。

## [0.6.17] - 2026-08-07

### 修正

- **`flush_map` 傳給 `multipath` 的參數形式從一開始就是錯的**。量測到的：

  ```
  multipath -f /dev/mapper/<wwid>   →  「device not found」，離開碼 1
  multipath -f <wwid>               →  離開碼 0，map 真的被移除
  ```

  那個 symlink 存在，而且指向正確的 dm 裝置；multipath 就是不接受那種形式。而錯的形式**被掩蓋了兩層**：程式把「device not found」當成成功——對它原本寫的那個情境是對的，因為 `fail_if_no_path` 之後 multipathd 可能已經先移除了 map——所以「從來沒發生的清除」和「已經完成的清除」長得一模一樣。然後 multipathd 又幫它遮著，因為沒有路徑的 map 會被自己清掉。只有在「storage 比 multipathd 先被移除」時才露出來。現在它傳名稱，**而且會再查一次 map 是不是真的不在了**，不相信離開碼：這是同一天第三次遇到「指令回報成功卻沒做事」。

### 已驗證

- **DSM 7.1.1 → 7.4.1-90080 升級保住了每一顆 LUN 的 uuid**，所以每一個 WWID 都活下來了。那是這個 plugin 最根本的假設——storage 的身分釘在 uuid 上——而它從來沒有經歷過大版本升級。這是拿 plugin 自己在升級前寫下的追蹤檔對出來的，不是拿筆記對的。
- **整台 NAS 中斷不會讓節點卡死**。管理連線與資料路徑第一次同時斷，約十四分鐘：**零個行程進入不可中斷睡眠**，`pvestatd`／`pvedaemon`／`pveproxy` 全部有回應，API 在 0.88 秒內回答，而**其他七個 storage 全部 active**——只有這一個 inactive。核心讓路徑失敗而不是排隊（`fast_io_fail_tmo 5`，然後 `no_path_retry 18`），那正是規則 4 存在的理由。NAS 回來之後 storage **自己**回到 `active`，沒有任何介入。
- **`_warn_once` 真的只警告一次**。整段中斷期間 `pvestatd` 每 ~10 秒輪詢一次，它的日誌裡關於這個 storage 只有**一**筆訊息。
- **一個新的錯誤碼：498，出現在 DSM 升級期間**。DSM 答得出 HTTP，但拒絕登入。它**不在**憑證錯誤集合裡，而那正是救了這個 storage 的東西：一個被閂鎖的憑證失敗會讓它一直死著，直到操作者跑 `pvesm set`。那是運氣好而不是設計，現在記下來了。

## [0.6.16] - 2026-08-07

### 已驗證

- **一台 DS925+，DSM 7.3.2-86009 Update 4，透過 VPN 完整跑過一輪**：`pvesm add`、`pvesm status`、建立 LUN、**建立 target**、iSCSI 登入、multipath（裝置由核心的 WWID 確認）、guest 在 6.4 秒開機、對執行中的 guest 拍快照 4.0 秒、**倒回 3.3 秒**（由陣列自己的 `restored_time` 作證，而不是靠「沒有出現錯誤」）、刪快照、`free_image`，以及把整個 storage 移除。**NAS 最後剩下 0 顆 LUN、0 個 target，節點沒有任何工作階段**——兩邊都沒有留下東西。

  這也是 0.6.10 改過憑證路徑之後第一次真正跑它：檔案以 `0600` 落在 `/etc/pve/priv`，而 `storage.cfg` 裡沒有任何密碼。那是這個專案自己的發行檢查清單要求的事，而先前有兩個版本正是漏了它才出貨即不可用。

  **沒有在那台上重複的**：擴充、複製、備份、還原、遷移、雙 portal multipath 與 HA 只在 DS918+ 上跑過。而那台是透過 VPN 連的——測試沒問題，但**不是**正式環境 guest 磁碟該待的地方，因為隧道斷掉等於拔掉排線。兩份 README 都寫了這句。

- **DSM 是在「有 target」的時候才開始聽 3260**。一台沒有 target 的 NAS 會直接拒絕那個埠。那既不是防火牆，也不是「要開的埠」：防火牆查過了，它對那個網段的所有連接埠都允許，而關閉的埠在兩台 NAS 上都回 RST。**建立 target 就是啟動那個服務的動作**，而 plugin 自己在 `_ensure_target` 裡做了——所以 `pvesm add` 完全不需要 iSCSI，第一顆磁碟就會把埠帶起來。量測到的：`pvesm alloc` 之前是 refused，之後立刻連得上。

## [0.6.15] - 2026-08-07

### 新增

- **第二個機型與第二個 DSM 版本已經回答了**（唯讀）：一台透過 VPN 連到的 **DS925+，DSM 7.3.2-86009 Update 4**。這個 plugin 用到的每一個 API 都存在，版本與 CGI 路徑都相同——**包括 `SYNO.API.Auth` 在 `entry.cgi`**，而不是兩份公開參考實作寫死的 `auth.cgi`。那正是「問 `SYNO.API.Info` 而不是用常數」要防的東西，而它從來沒有在第二個 DSM 版本上被檢查過。`max_iscsiluns`、`max_iscsitrgs`、`max_snapshot_per_lun` 與 `iscsi_target_type` 讀到的都和 DS918+ 一樣；`type=define` 在那台上有 346 個鍵，DS918+ 是 316。把關 `pvesm add` 的 Btrfs、機型支援度與儲存空間檢查全部通過。

  **資料路徑沒有在那台上跑過**——它拒絕 3260 埠——所以這是答案的 API 那一半，並且以那樣的身分被記錄。

### 修正

- **錯誤 117 的意思是「沒有這個儲存空間」，而 `syno-location` 打錯會被報成 NAS 連不上**。在 DSM 7.1.1 **與** 7.3.2 上都量測過：要求一個格式正確但不存在的儲存空間路徑會得到 117，而一個完全不是儲存空間形狀的路徑則回傳「成功但沒有 volume」。所以同一個操作失誤有兩條路，而 117 那條路會說「NAS 沒有回答」——而 NAS 答得非常清楚。`Health` 現在在「錯誤碼就是答案」的地方改用 `call` 而不是 `call_ok`——那個函式自己的註解一直都這樣寫著——並且以數字碼判斷，絕不看它選的字。
- **`has_acceptable_disk` 是健康的儲存空間狀態**。它的意思是那些碟不在 Synology 的驗證清單上，而任何裝第三方硬碟的 NAS 都會這樣回報。原本的檢查只跟 `normal` 比對，所以對每一個那種健康的儲存空間都會警告——而一個會誤報的守門員，是大家學會忽略的守門員。只加了這一個狀態，因為只有這一個被看到過；`crashed` 與 `degrade` 仍然會警告。

## [0.6.14] - 2026-08-07

### 文件

- **遷移殘留已經不會發生了，而繼續說它會發生，是在讓操作者去追一隻鬼**。先前有四處公開段落寫著：一台 VM 從 `pve1 → pve2 → pve3` 遷移後在 pve3 上被銷毀，較早的節點會各自留著一個對應已不存在 LUN 的 map。在兩個節點上、離線與線上、兩個方向重新量測：**VM 離開的那個節點有 0 個 map、0 個 by-path 裝置，追蹤檔是空的**。切換之後 VM 在來源節點停止時，`vm_stop_cleanup` 會呼叫 `deactivate_volumes`，所以來源節點「是」有被通知的。原本那次量測早於 `_detach_local` 的修正——舊版停在第一次 flush，會留下一個正確的卸離不會留下的 map。`pve-syno-reap` 的用途沒有變：被硬重置的節點依然完全不會執行 `deactivate_volume`。**沒有**重新量測的是原本那個完整情境，而它現在以推論的身分被記錄，不是以結果的身分。
- 兩份 README 都列出了**在一個五節點的正式叢集上、從網頁介面實際跑過的項目**——磁碟、快照、倒回、兩個方向的遷移、複製、範本、三種備份模式與兩條還原路徑——並且把區塊儲存 plugin 最容易做錯的兩個結果單獨寫出來：兩邊都沒有留下任何東西，以及刪掉範本不會弄壞它的連結複本。

## [0.6.13] - 2026-08-07

### 修正

- **從快照複製被拒絕，而拒絕它的是一道守著「半個錯的不變量」的防護。**`volume_has_feature` 宣告 `clone => { snap => 1 }`，而 `activate_volume` 對任何 snapname 都 die——所以 `qm clone --snapname <快照> --full 0` 會以「a snapshot cannot be activated; roll back to it or clone it」失敗，而操作者當下正在複製它。

  PVE 的 `clone_vm` 在複製之前會**帶著 snapname** 啟用來源磁碟——`API2/Qemu.pm` 裡的 `activate_volumes($storecfg, $vollist, $snapname)`——連結複製和完整複製都一樣。這個專案寫下來的不變量是「任何用 `snap` 宣告的能力，都必須在不用 `path()` 也不用 `activate_volume` 的情況下成立，因為兩者都拒絕 snapname」，而後半句讓前半句永遠到不了。

  修正後的不變量：`snap` 宣告必須在不用 **`path()`** 的情況下成立，而 `activate_volume` 對 snapname 必須是**成功地什麼都不做**。本來就沒有東西要啟用——Synology 的 LUN 在快照上沒有裝置，而 `clone_from_snapshot` 完全在陣列端——而 `deactivate_volume` 一直都是對 snapname 回傳 1。缺的是啟用那一半。`path()` 仍然拒絕，而且必須拒絕：真正需要「快照當下那顆裝置」的呼叫者必須大聲失敗，而不是拿到目前狀態的裝置。

  `t/11-features.t` 斷言的是錯的那個不變量，所以必須跟著程式一起改——這正是「把不變量寫成測試而不是寫成註解」的理由：那個信念一崩，測試當場就紅了。

### 文件

- 一小時前寫進文件的指令是錯的：**必須加 `--full 0`**。省略 `--full` 不等於關掉它——PVE 對任何「不是範本」的東西把它預設為「真」（`$param->{full} // !PVE::QemuConfig->is_template($conf)`），所以不寫它，要到的正好是那個被正確拒絕的完整複製。

## [0.6.12] - 2026-08-07

### 修正

- **倒回失敗、訊息是「no device appeared」，原因是 DSM 要求節點登出**。失敗當下的 journal：

  ```
  iscsid: connection1:0 Target requests logout within 10 seconds
  iscsid: connection1:0 is operational after recovery (1 attempts)
  iscsid: connection1:0 Target requests logout within 10 seconds
  iscsid: connection1:0 is operational after recovery (1 attempts)
  ```

  **DSM 在從快照還原 LUN 時會把工作階段拆掉**，七秒內兩次。`activate_volume` 只重新掃描工作階段**一次**，然後等二十秒等裝置出現——所以在那段震盪中間發出的掃描什麼也沒做到，而後面的等待永遠不可能成功。LUN 是對應好的，工作階段也回來了，手動掃描一次就立刻好了。現在改成每五秒重新發出一次、最多四十五秒，而且久到會被察覺時會警告一次。

  這是 `grow_map` 的教訓換一個層級，而且是同一天第三次：**發出一次指令然後輪詢它的效果，和「一邊輪詢一邊重發」不是同一件事。**

- **`t/07-imports.t` 沒有掃描 plugin 本身**，而那是這個專案最大的檔案——它被漏掉是因為載入它需要 Proxmox VE。它漏掉了同一小時內加進 `activate_volume` 的一個 `_warn_once` 呼叫：`perl -c` 靜靜編譯過去、測試也過了，而那個函式只存在於 `Health.pm` 而且是私有的。它會在執行期、在啟用路徑上、在倒回過程中死掉。現在只要有 Proxmox VE 就會掃描 plugin，而且已經示範過這道防護會在那個呼叫上失敗。

## [0.6.11] - 2026-08-07

### 修正

- **每一台正常關機的 VM 都無法倒回**，被 0.6.8 在三個版本前加的那道防護擋下。`flush_device_cache` 在「清空失敗」和「這個節點上沒有裝置」兩種情況都回傳裸的 `0`——而 PVE 要求磁碟倒回前必須關機，關機會停用磁碟區，所以後者才是常態。沒有裝置就沒有髒頁，那是最安全的狀態；它卻被當成失敗回報，倒回就停住了。兩個快取輔助函式現在都是三值——`1` 已清空、`0` 這裡有裝置但清空失敗、`undef` 這裡沒有東西要清——而三個呼叫點都先讀 `defined`。

  這是規則 12 的形狀出現在「為上一次違反規則 12 所寫的修正」裡面，而這已經是這個專案第二次發生。`t/05-multipath.t` 現在會斷言這個契約，並且掃描 plugin 原始碼裡有沒有對這兩個函式的裸否定。

## [0.6.10] - 2026-08-07

### 修正

- **taint 模式的另一半：檔案操作**。0.6.9 解除了送進**指令**的值的污染；而 `open` 寫入、`unlink`、`mkdir` 與 `rename` 受到一樣嚴的限制，而 storage id 會進到檔名的地方有**四個**——憑證存放、WWID 狀態檔、憑證閂鎖，以及清除它的那個函式。四個都用 `s///` 清理 id，而 `s///` **不會解除污染**：只有 capture 會。在 `-T` 底下用受污染的 storeid 探測，四個裡有三個死於「Insecure dependency in unlink」，而它們正是 `pvesm add` 與 `pvesm set` 從網頁介面走的路徑。現在只有一個 `Naming::filename_component`，而它把驗證和解除污染做成同一個動作——這正是 `slaves_of_map` 一直以來的做法。

  這是在 0.6.9 之後主動問「還有哪裡？」找到的，不是等回報。同一段程序寫了四次，是一再出現的形狀：`_fuser`／`scsi_id` 對上五個裸指令名稱、`slaves_of_map` 對上其他每一個受污染的參數，現在是這個。

- **`$ENV{PATH}` 裡不能有任何 taint 模式會拒絕的東西，而 0.6.10 自己那行「以防萬一」正是下一個問題**。只要 `$ENV{PATH}` 裡**任何一個**目錄是群組或其他人可寫，Perl 就拒絕 `exec`——「Insecure directory in `$ENV{PATH}` while running with -T switch」——而先前設給子行程的 PATH 是整份搜尋清單，包含 `/usr/local/bin`。在那個目錄是群組可寫的機器上，plugin 從 `pvedaemon` 或 `vzdump` 執行的每一個指令都會死。現在會過濾成 taint 模式接受的目錄，而且每次呼叫都重新檢查，因為目錄權限正是這個專案已經被抓到會亂假設的那種「節點本機事實」。

### 文件

- 容量單位的對照現在**看得到**了：上方 Proxmox VE 顯示 `28.12%（4.32 TB 於 15.36 TB）`，下方 DSM 顯示 `3.9 TB / 14 TB、28%`——同一個儲存空間，同樣的位元組。

## [0.6.9] - 2026-08-07 ——已標記，但從未發布

它的 CI 執行失敗，沒有建立任何 release，所以這個版本沒有任何東西可以下載，標籤也已經撤回。以下所有內容都在 **0.6.10** 出貨；這一節保留下來，是因為那些變更是真的，而且「為什麼沒有發布成功」值得記錄——`t/13-taint.t` 把 `-T` 放在 shebang 裡，而那要靠測試框架注意到它並把開關傳下去。

### 修正

- **Taint 模式。**`pvedaemon` 的 shebang 是 `#!/usr/bin/perl -T`，所以這個 plugin 從檔案讀進來的每一個值都是受污染的，而 Perl 拒絕用它 `exec`：「Insecure dependency in exec while running with -T switch」。multipath 的 map 名稱是從 `/sys/block/dm-N/dm/name` 讀來的，然後直接送進 `multipathd resize map <name>`，所以**每一個從網頁介面開始的擴充都失敗**——這是 0.6.7 修掉的「沒有 PATH」後面的第二道關卡，在前一道清掉之前根本看不到。`slaves_of_map` 本來就是從 regex **比對到**的結果取裝置名稱，而不是從讀到的東西取，它的註解把那叫做「taint 紀律與正確性檢查合而為一」——套用在那裡，其他地方都沒有。現在參數會在指令執行器上以允許清單驗證並解除污染，而一個這個 plugin 不可能產生的值會被**拒絕**，不是被清洗。
- taint 模式在 `exec` 之前也要求環境是乾淨的，所以除了 0.6.7 加的絕對 `PATH` 之外，`IFS`、`CDPATH`、`ENV` 與 `BASH_ENV` 也會為子行程移除。

### 新增

- **`t/13-taint.t` 在 `-T` 底下執行**，所以 `make test` 終於能碰到 daemon 真正執行的那個環境。這個專案先前完全沒有重現過它：`qm`、`pvesm` 與 `pvesh` 全都是普通的 `#!/usr/bin/perl`，這正是為什麼兩輪實機驗證都通過、而網頁介面照樣失敗。那行 shebang 就是測試夾具——把案例加到其他檔案裡並不能涵蓋它。已經在刻意製造的回歸上示範過會失敗。

## [0.6.8] - 2026-08-07

### 修正

- **快照與倒回前後的主機快取清空，現在會在沒有做到時說出來**。這兩個呼叫原本都只是為了副作用而呼叫、回傳值被丟掉——而那個回傳值是唯一能說出 `blockdev` 到底有沒有執行的東西，而從 PVE daemon 呼叫時它並沒有。所以**每一個從網頁介面拍的快照都靜悄悄地跳過了清空**，而倒回也跳過了這個專案量測過確實必要的快取失效：讀取會一直拿到倒回前的位元組，直到快取被丟掉為止。現在倒回在無法清空時會**拒絕**，快照則是**警告**（它仍然是毀損一致的，只是比預期舊一些），而倒回之後的失效也是警告，因為那時陣列端的工作已經完成了。

## [0.6.7] - 2026-08-07

### 修正

- **每一個外部指令現在都會解析成絕對路徑，理由是 Proxmox VE 的 daemon 根本沒有 `PATH`**。在這台節點上量測：**pvestatd、pvedaemon、pveproxy 與 pve-ha-lrm** 的 `/proc/<pid>/environ` 裡都沒有 `PATH` 這個變數，而整個 PVE 樹裡也沒有任何地方在執行期設定它。`exec` 於是退回 C 函式庫的預設值 `/bin:/usr/bin`——而 `multipathd`、`multipath`、`iscsiadm`、`dmsetup` 與 `blockdev` 全都在 `/usr/sbin`。所以**同一個操作**會因為是誰啟動的而成功或失敗：從 shell 上用 `qm resize` 做的擴充會成功，從網頁介面做的一模一樣的擴充則連 `multipathd` 都執行不了。這個 plugin 有五個指令是用裸名稱呼叫的。PVE 自己的 plugin 一直都寫絕對路徑，正是為了這個原因——`ISCSIPlugin` 寫 `/usr/bin/iscsiadm`，`LVMPlugin` 寫 `/sbin/vgs`——而這個 plugin 裡本來就自己解析路徑的兩個地方，`_fuser` 與 `scsi_id`，就是正確答案的形式，只是七個指令裡只套用到兩個。
- **一個從來沒有執行的指令，不再看起來像執行了然後拒絕。**`resize_map` 兩種情況都回傳一個裸 0，所以一個根本無法執行的 `multipathd`，和一個看過之後發現沒事可做的 `multipathd`，是分不出來的。上面那個問題就是這樣撐過整整 60 秒的重試迴圈：三百次失敗、沒有任何輸出，最後給出一個怪罪 map 沒有跟上路徑的錯誤。`grow_map` 現在會在第一次這種失敗就停下來，並說出到底是哪一種。

### 新增

- `make check-tool-paths` 會在任何繞過指令執行器的行程產生點上讓建置失敗，這樣之後新增的指令就不會是漏掉的那一個。它第一次跑就找到另外三個，而且已經在刻意製造的回歸上示範過會失敗。
- 探索工具不再用 `stty` 來關閉回顯。`POSIX::Termios` 是 Perl 核心模組，做的是同一個 `termios(3)` 呼叫，不需要尋找也不需要 exec 任何東西。

## [0.6.6] - 2026-08-07

### 修正

- **磁碟擴充只到了 NAS，沒有到節點。**`qm resize` 正確地把 LUN 擴大了，然後以 `qmp command 'block_resize' failed - Cannot grow device files` 失敗，留下 NAS 已經是新容量、而 VM 設定檔還宣稱舊容量的狀態。原因是：`multipathd resize map` 的新容量來自 multipathd 自己的 **udev 視圖**，而在觸發變更的 sysfs 重新掃描之後幾微秒，那個視圖還沒更新。multipathd 拿過期的容量和 map 的容量比對，發現一樣，記下「map is still the same size」然後以 **0** 結束。所以 map 從來沒有長大，也沒有任何東西回報有問題，而 PVE 的下一步——`block_resize`，對還沒跟上的裝置完全沒有容忍度——才是失敗的那一個。實機量測：NAS 把 LUN 擴到 33 GiB，`sdb` 也跟上了（`detected capacity change from 67108864 to 69206016`），而 `dm-0` 停在 67108864。現在會輪詢 map 直到它帶著新容量，過程中重新發出 resize，而**一個始終沒有到達節點的擴充會帶著說明失敗**，不再交給 QEMU 去產生一個不相干的訊息。

- **比自己 LUN 還小的 map 會在啟用時對齊。**`volume_resize` 只在擁有該 guest 的節點上執行，所以其他每一個節點都還呈現舊容量；即時遷移到其中一台，會把一個比它自己設定檔還小的裝置交給 guest。`activate_volume` 現在會比對兩者——LUN 的容量在那裡本來就已經拿到了，所以不需要多打一次 NAS——並且是警告而不是拒絕，因為啟用失敗會讓 VM 開不起來。

### 文件

- **為什麼 Proxmox VE 和 DSM 報的容量不一樣**。其實沒有不一樣：plugin 把儲存空間的 `size_total_byte` 與 `size_free_byte` 以位元組原封不動傳出去，沒有做任何運算，然後 PVE 除以 1000、DSM 除以 1024——兩邊都寫「TB」。15.36 TB 和 14 TB 是同樣的 15,356,124,401,664 位元組。
- 0.6.5 加入的快照描述，現在在兩份 README 和文件網站上都**看得到**。這是一個看圖比看字容易得多的改動。
- 修正文件網站上「SAN Manager 在 LUN → 快照 底下會列出快照**名稱**」的說法。它列出的是快照本身；任何地方都沒有名稱欄。

## [0.6.5] - 2026-08-07

### 修正

- **Proxmox VE 的快照名稱現在會出現在 SAN Manager 裡**。它的快照清單顯示時間、一致性狀態、描述、狀態、鎖定——**完全沒有名稱欄位**。`name` 這個欄位在 API 裡，plugin 也用它比對，但看著 DSM 的管理者看不到它：描述原本是 `Proxmox VE <storage>`，所以同一個 storage 上每顆磁碟的每個快照看起來都一樣，而「這一列是哪一個 PVE 快照？」在不回到 PVE 比對時間戳的情況下沒有答案。現在是 `<快照名稱> (Proxmox VE <storage>)`。

### 實機驗證

- **快照名稱：NAS 什麼都不拒絕**。256 個字元、空格、`_` `+` `@` `/` `%` `:`、開頭連字號、中文字全部被接受——這和 LUN 名稱是不同的規則，LUN 名稱會用 18990503 拒絕 `_`、空格、`+`、`@`。不需要任何淨化處理，而且 **PVE 才是比較嚴的那一端**：`pve-snapshot-name` 是 `pve-configid` 加 40 字元上限，所以到得了 plugin 的名稱只可能是字母開頭、後接字母數字底線連字號。不存在任何 PVE 能產生而 NAS 會拒絕的組合。
- 安裝命令現在使用 `/releases/latest/download/jt-pve-storage-synology_all.deb`，不會過期。`check-doc-urls` 會驗證它真的解析得到，因為它依賴的兩件事——發行版不被標記為預發行版，以及工作流程會發布一份不帶版本號的副本——都在發行工作流程裡，有可能在無聲之間不再成立。

## [0.6.4] - 2026-08-07

**從這個版本起，`beta1` 字樣全部拿掉**。版本號現在是單純的 `0.6.4`，tag 是 `v0.6.4`，套件是 `jt-pve-storage-synology_0.6.4-1_all.deb`。先前的版本保留它們的 `~beta1` 名稱——改寫歷史會讓引用它們的升級說明變成錯的。

有兩個值得知道的後果：

- **GitHub 的 `latest` 現在有用了**。發行工作流程原本會在 tag 含有 `beta` 時把該次發行標記為預發行版，而 GitHub 的 `latest` 會跳過預發行版——這就是為什麼 `/releases/latest/download/…` 會回 404，而文件裡的安裝命令必須帶版本號。從 0.6.4 起它是一般的發行版。
- **`.deb` 會以一個不帶版本號的名稱再發布一份**，`jt-pve-storage-synology_all.deb`，所以 `/releases/latest/download/jt-pve-storage-synology_all.deb` 在各版本之間都會是對的。帶版本號的那份也保留：`releases/` 是用那個名稱歸檔的，而測試者必須能取得他實際在跑的那一個。

這仍然是 0.x 版。1.0.0 在等的東西沒有變——第二個機型、第二個 DSM 版本、以及確定最小的 DSM 權限——而 `docs/TESTING_zh-TW.md` 仍然是那份驗證紀錄。

### 新增

- `--nodes` 有文件了：如何把 storage 限制在某幾個節點、分批上線時為什麼要這樣做，以及那個容易搞反的地方——`shared` 是強制開啟、不能關掉的，所以 `nodes` 限制的是**哪些節點可以使用它**，絕不會讓這個 storage 變成節點本機的。

### 文件修正，全部是在一台乾淨節點上安裝時發現的

- 安裝命令用了 `/releases/latest/download/…`，那會 **404**；接著用 `dpkg -i`，那**不會處理相依性**——在沒有 `multipath-tools` 的節點上它會解開套件然後留下未設定的狀態。現在改用 `apt install ./…`，並附上已經踩到的人的復原步驟。
- 前置套件從來沒寫。這是查出來的、不是猜的：`open-iscsi` 和 `multipath-tools` 有**零個** PVE 反向相依，所以節點真的可能沒有它們；那四個 Perl 模組各自是 86 到 151 個 PVE 套件的相依，一定已經在了。
- **storage 可能在網頁介面上看不到**。介面是由瀏覽器所連上的那個節點提供的，而沒有裝 plugin 的節點不認得這個類型，會把該 storage 從清單裡靜靜略過——那個症狀看起來就像 `pvesm add` 失敗了。
- 安裝警告原本寫 `multipathd reconfigure`「會重讀節點上每一個 map 的設定」，那是正確的，但讀起來像威脅。**改成實測**：在 multipath-tools 0.11.1 上，一邊對一個不相關的 map 持續讀取，1776 次讀取、0 次失敗，而那個 map 的 device-mapper 事件計數器完全沒有動——它從未被重新載入。

## [0.6.3~beta1] - 2026-08-07

**R-14 已在硬體上探測，而本專案先前對它的答案是錯的**。只有文件變更，沒有程式碼改動。

### 更正

- **非管理員的 DSM 帳號根本走不到「儲存呼叫被拒」那一步。它在登入就被拒絕。**`docs/DSM-ACCOUNT.md` 原本預測普通帳號會認證成功、然後每次 LUN 列舉都回 `105`。在擁有者同意下於測試 NAS 建立、探測、再刪除的一個臨時帳號顯示並非如此：

  | 步驟 | 結果 |
  |---|---|
  | DSM 7.1.1 上的群組 | 只有三個——`administrators`、`http`、`users`；**沒有任何 iSCSI 專用的** |
  | 完全沒有群組 | 登入被拒，**錯誤 402** |
  | 加入 `users`（從群組那一側確認）| 登入被拒，**402** |
  | `SYNO.Core.User get` | 只回傳 `name` 和 `uid`；沒有權限欄位 |
  | `Group.Member add` 到 `administrators` | **回報 `success: true` 而什麼都沒改** |

  所以無法透過 API 隔離出最小集合，而探測就此停止，不去對一台正式 NAS 上未公開的模型做猜測。現在誠實的說法是：**使用中的帳號是管理員，而沒有任何更小的可用設定被示範過**。剩下的一步是 DSM 介面上的檢查，`DSM-ACCOUNT.md` 裡寫下了要執行的確切命令。

- **第三個回報成功卻沒做事的 API**，加入了「拒絕 255 字元名稱卻仍然建立 LUN」以及「`multipath -w` 印出 `wwid ... removed` 而什麼都沒做」的行列。規則 35 說 `success` 是唯一的成功；這三個說的是，當結果可以獨立讀回來時，連 `success` 都還不夠——而使用者那一側的 `additional=["groups"]` 對一個**確實**在群組裡的帳號回傳空清單，所以「你問哪一側」也很重要。

## [0.6.2~beta1] - 2026-08-07

**節點故障、圍籬與接手**。在擁有者同意可以中斷的節點上執行，過程中維持法定人數。

### 修正

- **一次當機會留下一筆永遠不會有東西移除的追蹤記錄**。被硬重置的節點永遠不會執行 `deactivate_volume`，所以 `WwidState` 留著一筆對應「已不再掛載」LUN 的記錄——而那顆 LUN 在 NAS 上仍然存在，所以 `orphans` 正確地不會回報它。`reap_orphans` 現在把它當成一個獨立情況處理，並在報告裡說明。

  檢查用的是 `map_is_gone`，**不是** `device_path_for_wwid`：後者刻意把「沒有裝置」和「stat 沒有回來」都收斂成 undef，而用它來解除追蹤，正好會是 0.6.1~beta1 剛修掉的那個錯。只有確認為「已消失」才會解除任何追蹤。

  修正之前也不危險——每個使用者在動作前都會重新檢查裝置——但它是一筆聲稱節點持有某物而實際上沒有的記錄；而這件事交給 `pve-syno-reap`，不放在 `activate_storage` 路徑上，因為那條路徑不得改動任何東西。**節點當機之後，請跑一次 reaper。**

### 實機驗證

| 測試 | 結果 |
|---|---|
| 正常重開機，VM 執行中、LUN 已掛載 | 沒有殘留 map、工作階段或追蹤；VM 重新啟動，資料完好 |
| 硬重置（`sysrq-trigger b`），完全沒有清理 | 沒有殘留 map 或工作階段；留下上述那筆追蹤記錄 |
| VM 交給 `ha-manager` 的 HA 圍籬 | 節點在圍籬時間窗內重新加入，HA 在原地重啟服務，儲存自行重新掛上，經過帶 fsck 的當機開機後資料完好 |
| **在當掉的節點還沒回來時接手** | 存活節點在 **3.6 秒**內啟動 VM，而死掉節點的工作階段仍登記在 NAS 上；guest 開機，資料完好 |
| 當掉的節點回來之後 | 儲存 `active`，沒有 map、沒有工作階段，而且沒有去爭奪那顆磁碟 |

接手那一項才是關鍵：在當機的那一刻，一個 `max_sessions=0` 的共用 target 上有**兩個已連線的工作階段**，而存活的節點沒有等任何東西逾時就掛上了那顆 LUN。這正是正式叢集所依賴的，而它從來沒有被示範過。

## [0.6.1~beta1] - 2026-08-07

**一次配置會持有 Proxmox VE 叢集層級的 storage 鎖 3.6 秒。現在是 2.16 秒**。在三個節點同時發出十五個配置時，原本會有一個在等鎖時失敗。

### 修正

- **每次配置有三次完整的 `LUN list` 和兩個 DSM 工作階段，而且全都在 `cluster_lock_storage` 裡面**。那個鎖會把同一個 storage 上的每次配置在整個叢集範圍序列化，所以一次花多久，其他節點就要等多久。那份清單被 `find_free_diskname` 取用一次（它經由 `list_images`，因此會自己開一個工作階段——第二次登入、第二次 API 探索、第二次登出），LUN 上限檢查為了一個數量又取一次，接近上限的警告又取一次。現在 `alloc_image` 只取一次並傳給三者；`find_free_diskname` 接受一個選擇性的清單參數，沒有它時行為和基底類別完全相同。

  | 每次配置 | 之前 | 之後 |
  |---|---|---|
  | 實際耗時 | 3.6 秒 | **2.16 秒** |
  | DSM 工作階段 | 2 | **1** |
  | HTTP 請求 | 17 | **12** |
  | `LUN list` 呼叫 | 3 | **1** |

- **`wait_unlocked` 用 `sleep 1` 輪詢**，而驗證紀錄記錄 `is_action_locked` 在快照後 **0.20 秒**、1 GiB 建立後 1.2 秒就清除。五分之一秒要花掉整整一秒，每一次建立、每一次快照，而且都在叢集鎖裡面。現在它以 0.2 秒輪詢。

- **0.6.0~beta1 自己的修正裡，對一個三值答案取了否定。**`_detach_local` 的殘留路徑移除原本寫成 `if (!map_is_gone($wwid))`，而 `map_is_gone` 回傳 1 / 0 / **undef**——所以「無法判斷」被讀成「還在」，於是在沒有任何東西確認過的狀態下刪掉了 `sd` 裝置。這不是理論：`qm stop` 之後接 `qm rollback` 就以「no device …appeared on this node after logging in」失敗。那個分支現在要求 `defined $gone && !$gone`。

### 實機驗證

| 測試 | 結果 |
|---|---|
| 三個節點同時 15 個配置 | 37 秒，15/15，沒有重複名稱 |
| `qm move_disk` 搬離 storage 再搬回來 | 兩個方向都成功；guest 從搬回來的磁碟開機 |
| 對**執行中**的 guest 拍快照，然後倒回 | guest 開機後看到的是快照前的內容 |
| guest 持續寫入時擋掉 DSM 管理連接埠 | 6.3 秒變 `inactive`，只警告**一次**，**guest 裡 0 個 I/O 錯誤**，第一次輪詢就恢復 |
| `syno-min-free` 設得比 NAS 可用空間高 | 配置被拒絕，並附上數字 |
| 要求 1 MiB | 建立出 1 GiB——API 並不強制的最小值 |

收尾時 NAS 回到原本的四個 LUN 和三個 target，而每個節點都沒有 map、工作階段、node 記錄、憑證或 drop-in。全程沒有動到 `/etc/pve/priv/storage/` 裡另一套儲存的憑證檔案。

## [0.6.0~beta1] - 2026-08-06

**備份、還原、一台 guest 從 NAS 開機、跨三個節點即時遷移**。最後三個重要的空白都關掉了，而關掉它們的過程又找出兩個缺陷。

### 新增

- **`pve-syno-reap`**——回報（加上 `--remove` 則清除）本節點為 NAS 上已不存在的 LUN 所持有的 multipath map。預設是試跑。它絕不動到正在使用中的裝置，對任何無法確定狀態的東西是跳過，不是假設。

### 修正

- **`WwidState::orphans` 沒有任何呼叫者**。它就是為下面那個情況寫的，註解寫得很長，而完全沒有地方呼叫它——一段代替修正存在的死程式碼。`reap_orphans` 就是那個呼叫者。
- **`_detach_local` 無法 flush 一個「LUN 是從其他節點被刪除」的 map**。這個節點的 iSCSI 工作階段還開著，所以每個 `sd` 節點會以死掉的裝置留下來，而 multipathd 會在它上面重建一個 map。`free_image` 會先取得 slave 清單、flush、移除殘留路徑、再 flush 一次；`_detach_local` 停在第一次 flush——同一段程序被寫了兩次，而只有一份是對的。現在它會在、而且只在 flush 失敗時移除殘留路徑，所以一般的 VM 停止流程沒有改變。

### 更正

- **Proxmox VE 裡沒有任何東西呼叫 `deactivate_storage`**。這是對整個 `/usr/share/perl5/PVE` 目錄樹驗證過的：只有分派函式和各 plugin 自己的實作存在。所以這個專案原本那句「`pvesm set --disable 1` 會讓每個節點在下一次輪詢時停用」是錯的，而同一個下午寫下的一段註解聲稱 PVE 會「在這個節點用完這個 storage 時」呼叫它，也是錯的。相關專案 NetApp 那支早就發現並修正過完全相同的說法。文件裡的移除程序現在會先在每個節點上執行 `pve-syno-reap`。

### 實機驗證

一台 cirros guest 從 Synology LUN 開機，在三節點叢集上，全程 TCG（三台都沒有 KVM）：

| | |
|---|---|
| Guest 從 LUN 開機 | 掛上 `sda1` 並把檔案系統擴充到填滿它 |
| `vzdump` snapshot／stop／suspend | 13 秒／19 秒／15 秒，全部成功 |
| `qmrestore` 到新 VMID、開機、驗證 | payload `md5`**完全相同** |
| 即時遷移 pve1 → pve2 → pve3 | 中斷 **5 毫秒**、然後 **87 毫秒**，`/proc/uptime` 全程連續 |
| Guest 重新開機 | payload 完好 |
| 三個節點、一個共用 storage | 全部 active，全部用複寫過來的憑證登入成功 |
| `/etc/pve/priv/storage/` 複寫 | 三個節點都確認，權限 `0600` |

收尾時 NAS 回到原本的四個 LUN 和三個 target，沒有任何 `pve-` 開頭的東西，而每個節點都沒有 map、工作階段、node 記錄、憑證或 drop-in。

## [0.5.5~beta1] - 2026-08-06

**0.5.3~beta1 和 0.5.4~beta1 無法使用。`pvesm add` 會直接失敗**。請升級跳過它們。

### 修正

- **明明提供了密碼，`pvesm add` 卻回報「missing value for required option 'syno-password'」。**`extract_sensitive_params` 會在 `check_config` 驗證**之前**把每個機密屬性從參數裡移除——PVE 的順序就是這樣——所以那個必填檢查在找一個已經被拿走的東西。機密屬性必須宣告成 `optional => 1`，並由 plugin 自己檢查它有沒有被提供；PVE 自己的 CIFS plugin 就是這樣做的。任何單元測試都抓不到這個：問題出在 PVE 兩個階段之間的互動，而它是在實際執行的第一分鐘被發現的。
- **CHAP 只在 target 被建立時才會套用**。把 CHAP 加到一個 target 已經存在的 storage 上，既沒有錯誤也沒有存取控制——NAS 上那個 target 維持 `auth_type=0`，而設定卻說有。這是實機量到的。現在 `Target::ensure` 會拿陣列回報的狀態來對照 CHAP，而更新 hook 會把密鑰推送到這個 storage 擁有的**每一個** target，因為在 `per-volume` 模式下每顆磁碟各有一個。陣列永遠不會回傳密碼，所以改變過的密鑰只能推送、無法比對。
- **設定了 CHAP 帳號但沒有密鑰，原本會被接受並只給一個警告**。現在會在寫入任何東西之前拒絕，而 `pvesm set` 完全不會動到設定。
- **`pvesm set --delete syno-chap-username,syno-chap-password` 拒絕了自己**。PVE 會在 hook 回傳**之後**才套用刪除，所以 `$scfg` 仍然帶著那個帳號，而憑證儲存區已經把密鑰移除了。現在 hook 會算出「有效設定」——目前的設定，減去刪除，加上新值。
- **一個無關的 `pvesm set --disable 1` 會回報「CHAP REMOVED from 1 target(s)」**，而那個 storage 根本沒有 CHAP、也沒有失去任何東西。現在對照動作會跳過陣列已經回報「沒有 CHAP」的 target。

### 實機驗證

0.5.2 到 0.5.5 的每一項變更都對這台 DS918+ 跑過：密碼不在 `storage.cfg` 而在 `/etc/pve/priv/storage/pvesyno.syno`（0600，`www-data` 被拒絕），API 不回傳任何憑證欄位，配置、啟用、區塊層級倒回（8 MiB 隨機資料寫入後歸零再倒回，`sha256` 前後相同）、從快照複製、`copy` 對快照正確拒絕而 `clone` 允許、縮小拒絕、名稱已存在拒絕、連續十二次失敗操作而工作階段沒有耗盡，然後釋放、停用、移除——**最後 NAS 回到原本的四個 LUN 和三個 target，沒有留下任何 `pve-` 開頭的東西，節點上也沒有 map、工作階段、node 記錄或 drop-in。**

### 記錄下來，但沒有「修正」

在 multipathd 執行中的情況下，`multipath -w <wwid>` 會印出 `wwid '<...>' removed` 而**什麼都沒改**；`multipathd del wwid` 回答 `fail`。所以這個 plugin 附加到 `/etc/multipath/wwids` 的 WWID 在卸離時不會被移除，而且沒有任何地方聲稱它們被移除了。`-W` 會有作用，但永遠不會被使用：測試節點上那個檔案有 334 筆，其中 319 筆屬於另一套儲存。殘留是每個曾經掛上的 LUN 一行，而且不會造成誤導，因為 WWID 是從 LUN 的 uuid 推導出來的，只可能對應到它原本那顆 LUN。

## [0.5.4~beta1] - 2026-08-06

**第三輪稽核，而它三個發現裡有兩個是關於第二輪的**。這就是「改完東西之後再跑一次」而不是「改之前跑一次」的理由。

### 修正

- **0.5.3~beta1 搬移憑證的動作無聲地弄壞了 CHAP**。把 `syno-chap-password` 設為機密屬性，代表 PVE 會把它從設定裡拿掉，所以 `$scfg->{'syno-chap-password'}` 是 undef——而兩個 CHAP 呼叫點還在從那裡讀，兩個使用端最後又都是 `$opt{chap_password} // ''`。結果不是失敗，而是 target 上和節點 iSCSI 記錄裡的一個**空 CHAP 密鑰**：一個自稱已經設定好、卻誰都放進來的存取控制。現在只有一個存取函式，而 `Target::ensure` 和 `ISCSI::node_add` 都不接受缺少的密鑰——共用密鑰沒有安全的預設值，所以兩邊都會帶著原因拒絕。

  **沒有人用這個版本設定過 storage**，因為它是靠稽核那次修改本身查出來的，而不是靠實際跑出來的。如果你在 0.5.3~beta1 上設定過 CHAP，請重新設定 `--syno-chap-password`，並到 SAN Manager 檢查該 target 的密鑰。

- **`copy` 不再對快照宣告為可用。**`qm clone --full --snapshot <name>` 會帶著快照名稱去問 PVE 的 `copy` 功能；回答「可以」就會讓它對 `path(..., $snapname)` 執行 `qemu-img convert`，而那會死掉——一個 Synology LUN 在快照上沒有裝置，除非先複製或倒回。所以 PVE 會開始一個操作然後中途失敗，報出來的是定址的問題，而不是它被要求做的事。現在它一開始就拒絕，而它建議的動作——從快照做連結複本——是支援的。`clone => { snap => 1 }` 沒有變，而且是正確的。

### 變更

- **快照時間戳改為驗證，而不是斷言**。註解原本說 `create_time` 已經「對 NAS 自己的時鐘確認過」；驗證紀錄裡沒有這樣的量測，而且它**無法**用唯讀方式取得，因為 LUN 根本沒有 `create_time` 這個欄位。它現在公開列為 R-25。落在 2001–2065 之外的值會得到「沒有時間戳」，而不是一個標成 1970 的時間戳，而毫秒值會被換算。Proxmox VE 9 裡沒有任何東西會讀這個值——`Replication` 和 `QemuServer` 用的是快照名稱和一個 `parent` 欄位——所以今天這不會改變任何行為。

### 新增

- `t/11-features.t`——總共 283 個測試。它會解析那張功能表，並斷言只有 `clone` 和 `snapshot` 對快照宣告為可用，所以任何新增的 `snap` 鍵都會強迫人回頭看那兩個讓這種宣告成立的拒絕。

## [0.5.3~beta1] - 2026-08-06

**一個安全性修正，而且需要已經在使用的人做一個動作**。DSM 密碼原本被寫進 `/etc/pve/storage.cfg`。

### 安全性

- **DSM 憑證不再存放在 `storage.cfg`**。它們改放在 `/etc/pve/priv/storage/<storage>.syno`，權限 `0600`，而該目錄由叢集檔案系統只提供給 root——這也是 Proxmox VE 自己的 CIFS、PBS 和 ESXi plugin 存放憑證的地方。

  `storage.cfg` 是 `root:www-data 0640`，而且更糟的是：一個 Proxmox VE 不知道是機密的屬性，會透過 `GET /storage/<id>` 回傳給**任何持有 `Datastore.Audit` 的使用者**。一個唯讀的稽核者就能讀到一組具備 SAN Manager 權限的 DSM 憑證。而 device token——一個常設的 2FA 旁路——也是用同樣的方式存放的。

  PVE 對這件事有一套正式機制 `sensitive-properties`，而這個 plugin 沒有宣告它。當 plugin 沒有宣告時，`sensitive_properties` 會退回一份寫死的清單（`encryption-key keyring master-pubkey password`），而 `syno-password` 不在裡面——所以這個遺漏是無聲的，而且是朝著最不安全的方向無聲，這也是它能撐過十五個版本的唯一原因。

  **如果你正在升級，請對每個 storage 做一次：**

  ```bash
  pvesm set <storage> --syno-password '<密碼>'
  ```

  這會把它搬到私有檔案並從 `storage.cfg` 移除。舊位置仍然會被讀取，所以在你處理到它之前不會壞掉，而 plugin 會警告一次。因為那個值任何以 `www-data` 身分執行的東西都讀得到，**請視為已經洩漏，並更換該 DSM 帳號的密碼**，如果有用 2FA 也請撤銷 device token。完整步驟見 `docs/DSM-ACCOUNT_zh-TW.md`。

### 修正

- **九個方法在錯誤路徑上洩漏 DSM 工作階段**。有二十個方法會建立 API 物件，其中九個在建立與 `logout` 之間有 `die`。R-13 已經確認第二次登入不會踢掉第一次，所以它們會累積在 NAS 上——一個反覆失敗的操作，每試一次就洩漏一個。現在物件會在 `DESTROY` 裡登出，而 Perl 在 die 路徑上也會執行它。在那九個呼叫點各自修一次，等於九次忘記的機會，而且對還沒寫出來的第十個方法沒有任何保護。
- **縮小被無聲地略過，而不是被拒絕。**`resize` 在 `size >= 要求值` 時直接短路，回傳未變更的 LUN——對一件沒有發生的事回報成功。Proxmox VE 不管 plugin 回傳什麼，都會把**要求的**大小寫進 VM 設定，所以設定會宣稱一個 NAS 上並不存在的大小，而下一次「增加 N」還會從那個數字算起。現在會拒絕，並且說明原因。
- **「被拒絕後刪除」那條路徑只用「缺少某個錯誤碼」來證明物件是新的**。DSM 拒絕建立卻仍然建立了，是實測到的行為，而清理動作會刪掉查詢找到的東西；而區隔「刪掉 DSM 剛做出來的東西」和「刪掉一個原本就存在的 LUN」的，只有錯誤碼不是 18990538 這一件事。現在 `create` 會先查名稱，若已被占用就直接拒絕，所以清理只會動到它自己建立的物件。R-9 已經確認 `LUN list` 不回報總數，所以一份被無聲截斷的清單是通往錯誤分支的真實路徑。
- **`on_delete_hook` 會留下憑證檔案**，包含在 NAS 連不上而提早 return 的那條路徑上。現在有一個作用域守衛，在每一條離開路徑上都會移除憑證與鎖定檔。

### 新增

- `t/10-credentials.t` 與 `t/09-session.t`——總共 249 個測試。憑證測試會實際驅動 PVE 自己的 `sensitive_properties` 和 `extract_sensitive_params`，所以斷言的是 PVE 真的把值拿掉了，而不只是 plugin 要求它這樣做。

## [0.5.2~beta1] - 2026-08-06

**一次稽核查出三件讀程式碼沒有查出來的事**。三件都是同一類：靠推論成立、而不是靠檢查成立的程式碼。

### 修正

- **`Command` 的函式可以被當成方法呼叫，而答案會是安靜的錯答案。**`Naming` 和 `Multipath` 在被以 `Module->function(...)` 呼叫過之後就加上了防護——那種寫法會讓參數整體位移一格，而且不會有任何錯誤。`Command` 是這個形狀的第三個模組，卻沒有防護：`Command->is_block_device($dev)` 會把類別名稱綁到路徑上，然後對一個確實存在的裝置回答**「不是區塊裝置」**。這比前兩個更糟：它把一個安全問題答錯了，而且是朝著採取動作的方向錯。`is_block_device`、兩個 sysfs 輔助函式和命令執行器現在都會直接拒絕方法呼叫。
- **成功路徑上一個沒有時限的 `waitpid`。**`sysfs_read_with_timeout` 先清掉自己的 alarm，然後卡在 `waitpid($pid, 0)`。就推論而言是安全的——要走到那裡必須先有 EOF，代表子行程已經把資料寫出——但一個只是被暫停、還沒死掉的子行程會讓它永遠卡住，而這個模組存在的理由正是不要那樣推論。現在它有時限了，而且不送訊號：子行程已經把事情做完了。
- **所有權閘門只存在於 `free_image`。**`volume_snapshot_rollback` 會覆寫整顆磁碟，而它原本只靠 `taken_by` 白名單。這個推論是成立的，但規則要的是一個檢查。現在兩條快照路徑都直接呼叫 `is_pve_managed_volume($name, $storeid)`。

### 變更

- **`make critic` 通過了，而且現在是 `release-check` 的一部分**。它從來沒有通過過：120 個發現，其中絕大多數是這份程式碼刻意違反的規則——`return undef` 正是三值安全契約所需要的，而 `_not_a_method` 防護必須在解開 `@_` 之前先讀它。`.perlcriticrc` 只關掉這些，並且在每一條旁邊寫下理由，然後又證明它仍然會在一個故意製造的退化上失敗。一個放在命令清單裡卻無法通過的守門機制，就是一個沒有人會去跑的守門機制。
- **對現行 PVE 重新執行基底類別掃描**：有 21 個方法會走到 `filesystem_path` 或 `$scfg->{path}`，其中 20 個已覆寫。第二十一個 `list_volumes` 是靠結構而安全的——只宣告了 `images` 和 `rootdir`，兩者都會走到已覆寫的 `list_images`——這件事現在被記下來了，好讓下一次掃描不會再去追它。
- **R-10 縮小了**。測試用 NAS 上唯一啟用的快照排程都是**共用資料夾**快照，那不可能出現在某個 LUN 的快照清單裡，而 `SYNO.Core.ISCSI.LUN.Snapshot.Schedule` 並不存在。過濾是針對 `taken_by` 的白名單，所以來源不明的一律預設排除。

### 新增

- `t/08-command.t`——總共 203 個測試。它示範了有時限的回收能在子行程被暫停時存活下來，以及方法呼叫防護確實會拒絕。

## [0.5.1~beta1] - 2026-08-06

**真正的 multipath，以及一次真實封鎖所揭露的缺陷**。把 NAS 的兩個位址都設定為資料 portal——這是第一次真正走多路徑的測試，並且在負載下注入了故障切換。

### 修正

- **被拒絕的憑證每次輪詢都會重試，而不是只試一次**。那個用來阻止重複失敗登入的鎖定原本是一個物件欄位，而 plugin 每次呼叫都會建立新的 API 物件——所以它跟著物件一起消失了。三次輪詢就是三次失敗登入，而那正是 DSM 自動封鎖的門檻。它現在是 `/run` 底下的一個檔案，並且由 `on_update_hook` 清除，因為設定變更代表管理者已經有機會把問題修好。經過五次輪詢驗證：只有一次登入真的送到 NAS，另外四次在本地就被拒絕。

  查出這件事的代價是測試叢集上的一個節點被真的封鎖了，而這是這道防護所能得到的最清楚的證據：被封鎖的節點收到 **407（IP 被封鎖）**，同時另一個節點正常登入，而 iSCSI 資料路徑全程照常運作——這正是管理者會完全忽略它的原因。解除的位置是控制台 → 安全性 → 帳號 → 自動封鎖 → 允許／封鎖清單。

### 已驗證

- **兩條路徑，故障切換零 I/O 損失**。在節點端擋掉其中一個 portal：該路徑在約 15 秒內變成 `failed faulty offline`，存活的那條維持 `active ready running`，**60 次讀取中失敗 0 次**，而解除封鎖後約 10 秒該路徑自動恢復。
- **管理連線中斷不會把資料一起帶走**。擋掉 DSM 的 5001 連接埠、放行 3260：storage 回報 `inactive`，節點上其他 storage 不受影響，`pvesm status` 只增加大約一個 `syno-status-timeout` 的時間，警告只出現一次而不是每次輪詢都出現，磁碟仍然可讀，而且它自己恢復了。

## [0.5.0~beta1] - 2026-08-07

**兩個節點，以及停機 3 毫秒的線上遷移**。在一個跑不同核心的兩節點叢集上驗證，同一顆 LUN 同時掛在兩邊。

### 新增

- **plugin 不再期望 multipath map 會自己存在。**`find_multipaths` 是逐節點的政策：一個測試節點是 `no`，為每個裝置都建 map；另一個是 `yes`，只為有兩條以上路徑的裝置建——所以單一 portal 的 LUN **完全沒有 map**，而這個 plugin 交出去的路徑指向不存在的東西，儘管工作階段是通的。`ensure_map` 現在會用 `multipath -a` 附加那一個 WWID、要求建立 map，如果還是沒有就**讓啟用失敗**，而不是讓一台 VM 對著不存在的路徑開機。
- `debian/control` 現在描述的是這個套件實際包含的東西，並且相依於 `proxmox-ve`、`open-iscsi`、`multipath-tools`，不只是建議安裝。

### 在兩節點叢集上驗證

- **同一帳號、兩個位址、兩個 DSM 工作階段同時存在**——驗證紀錄裡最後一個叢集問題，在真實叢集裡得到解答：它們並存，沒有任何一個被擠掉。
- `max_sessions = 0` 的同一個 target 上兩個 iSCSI 工作階段，NAS 兩個都列出來；兩個節點讀到相同的位元組；`shared` 被強制為 1。
- 離線遷移 2 秒，沒有複製磁碟。**線上遷移停機 3 毫秒**，來源節點在目的節點接手時釋放了裝置。
- 兩次遷移之後資料完好，而且在 VM 執行中拍的快照倒回後資料正確。

### 已記載

- **移除共用 storage 會在其他節點留下工作階段。**`on_delete_hook` 只在一個節點上執行，其他節點永遠不會被通知，因為 storage 在 PVE 有機會在那裡停用它之前就已經從設定裡消失了。這是 PVE 的架構形狀，不是 plugin 能修的東西，所以流程被記錄下來——先停用、等待、再移除——並附上逐節點的清理命令。

## [0.4.1~beta1] - 2026-08-07

一台真正的 VM 從 Synology LUN 開機了，而倒回也在**區塊層面**得到驗證：寫入一段內容、拍快照、用零覆寫、倒回，原本的 sha256 回來了。做了兩次——第二次完全沒有手動處理快取。

### 新增

- **主機快取對稱性**。快照前先 flush，所以它記錄的是客體認為自己寫下的內容。倒回前 flush、倒回後**失效**——後者是直接示範出來的：在一次成功的倒回之後立刻讀取裝置，回來的是**舊**的位元組，直到快取被丟掉為止。
- **使用中檢查**，移植時保留了它的三值契約：1、0、或 **undef**，而 `free_image` 與 `volume_snapshot_rollback` 遇到 undef 一律拒絕。對著一台真的開機中的 VM 驗證過——QEMU 握著磁碟時兩者都拒絕，訊息指出用 `fuser -vm` 查。
- `volume_size_info`、`volume_export`、`volume_import`，全部由 NAS 回答。匯出與匯入已透過 `pvesm` 完成位元組完全相同的往返。
- `rename_snapshot`、`get_subdir`、`prune_backups` 會用操作者能據以行動的訊息拒絕，而不是落到基底實作、產生一個關於路徑的訊息。
- `t/07-imports.t`——`perl -c` 對呼叫未定義副程式一聲不響，而那樣的一個呼叫曾經一路撐到一台開機中的 VM。

### 修正

- **`qm destroy` 每一次都默默漏掉它的 LUN。**`path()` 必須回傳 `($path, $vmid, $vtype)`——第二個元素是擁有者。回傳葉節點名稱會讓 PVE 自己的檢查變成 `"vm-9999-disk-0" != 9999`，於是它提早返回、**從來沒有呼叫 `vdisk_free`**。沒有任何東西失敗，唯一的痕跡是一句數值警告。
- **`qm create` 掛上既有磁碟時還沒開始就死了**。基底的 `volume_size_info` 會對 `filesystem_path` 執行 `qemu-img info`，而區塊儲存沒有那種路徑——而且錯誤訊息指的是 `filesystem_path`，不是真正的原因。這是靠掃描基底類別、找出**預設實作**會去碰 `filesystem_path` 或 `$scfg->{path}` 的方法找到的；有五個沒有處理。
- 移植過來的使用中檢查呼叫了 `dirname()` 卻沒有 `use File::Basename`，而另一處只複製了 `_untaint_device_path`、沒有複製 `_untaint_device_name`。

## [0.4.0~beta1] - 2026-08-07

**plugin 可以用了。**`pvesm add synologysan` 註冊成功，而且每一個生命週期操作都已對一台 DS918+（DSM 7.1.1）完整跑過——跑了兩輪，節點與 NAS 上都沒有留下任何東西：

```
add → alloc → activate → snapshot → rollback → clone → resize → free → remove
```

仍然是預發行版：一個節點、一個機型、一個 DSM 版本，而且還沒有任何 VM 真的從它開機過。

### 新增

- `SynologySANPlugin`——PVE 儲存 plugin。`path()` 回傳 dm-uuid 連結而不是 `/dev/mapper/<wwid>`，因為在 `user_friendly_names yes` 的節點上後者並不存在。每一次啟用都會用**核心的** WWID 確認裝置才使用它，因為 `mapping_index` 會重用，而殘留的路徑會指向另一顆磁碟。`volume_rollback_is_possible` 刻意回傳 1：和相關專案那些陣列不同，這裡的倒回會保留較新的快照，而且 LUN 的 uuid 不變。
- `on_add_hook` 會拒絕「id 折疊後與既有 storage 在同一台 NAS、同一個儲存空間上撞到同一個 LUN 前置字串」的 storage——那之後兩者無法區分，而且各自都能刪掉對方的磁碟。
- `on_delete_hook` 會移除這個 storage 自己的 target 並讓本節點登出；`deactivate_storage` 則在任何其他節點上、當那裡已經沒有屬於我們的東西時做同一件事。

### 修正——五個全都是「跑起來」才找到的，不是「讀出來」的

- **`status()` 回傳的是 `(total, available, used, active)`**，不是直覺的順序。順序錯了，NAS 的可用空間會出現在 Used 欄；更糟的是 `syno-min-free` 會拿**已用**空間去比較，所以那道防止 Btrfs 儲存空間被寫滿的守衛，讀的是完全錯的數字。
- `pvesm alloc` 傳的磁碟名稱是**空字串**而不是 undef，所以預設值從來沒有生效，LUN 名稱只剩下前置字串。
- **擴充從來沒有傳到節點**。重新掃描必須送到 map 底下的 slave `sd` 裝置；`/sys/block/dm-N` 沒有 `device/rescan`，而對不存在的檔案回傳 0 把這件事藏起來了。
- **對應到既有工作階段的 LUN 需要 `--rescan`**。登入只會發現當下已對應的 LUN，所以第一次啟用可以，之後每一次都找不到裝置。
- **兩種殘留。**`free_image` 留下一個過時的 multipath map，因為工作階段還在時，刪除 LUN 不會移除 `sd` 節點。而 `pvesm remove` 留下本節點仍然登入著一個它剛剛刪掉的 target。

## [0.3.1~beta1] - 2026-08-06

### 變更

- **PVE 儲存類型名稱是 `synologysan`，不是 `synologyiscsi`**。這個家族裡沒有任何一個 type 把協定寫進名字——`dellpowerstore`、`dellpowervault`、`dellunity`、`netappontap`、`purestorage`——而 Dell EMC 那個用一個 `dell-protocol` 選項同時涵蓋 iSCSI、FC、SAS 與 NVMe。風險不在於不一致：**type 名稱一旦有人用過就改不了**，而有些 Synology 機型確實支援 NVMe/TCP，屆時 `synologyiscsi` 會是一個錯的、又改不動的名字。`synologysan` 是廠商加產品——SAN Manager，正是本 plugin 驅動的子系統——並保留 `syno-protocol` 選項給未來。
- 現在改，正是因為現在改零成本：plugin 還沒寫，所以世界上沒有任何 `storage.cfg` 寫過這兩個名字之一。

### 新增

- `Synology::WwidState`——逐節點記錄這個節點認領過的 WWID，好讓 `free_image` 能在 LUN 被刪除**之前**清理裝置，那時還有東西知道哪個裝置屬於它。讀取時逐筆驗證：這個檔案會被無人看管的回收路徑讀取，而一行損壞的內容不可以變成一道指令。
- `Synology::Health`——`status()` 回答的內容。容量來自**一次**儲存空間讀取，絕不由加總 `allocated_size` 得出（reflink 的區塊會對兩顆 LUN 各算一次），加上 `pvesm status` 無法表達的 LUN 數量壓力警告，以及在 storage 被加入時就檢查的前置條件——而不是等到第一次拍快照才發現。

## [0.3.0~beta1] - 2026-08-06

### 新增

- `Synology::ISCSI`。**絕不使用 `iscsiadm -m discovery`**：一次 SendTargets 探索會為 NAS 上**每一個** target 建立節點紀錄，包含擁有者自己的、以及一個 Proxmox Backup Server 正在使用的；而在 `node.startup = automatic` 的節點上，那些紀錄會在開機時被登入。節點紀錄改為直接建立，一次一個 target、一個 portal。
- **機型自己的上限，從 NAS 讀取而不是看規格表。**`SYNO.Core.System info type=define` 在 DS918+ 上回報 `max_iscsiluns` **256**、`max_iscsitrgs` **128**，而技術規格表寫的是 512 與 256。兩份公開的參考實作都沒有讀它。
- plugin 現在會在 NAS 之前就拒絕建立 LUN，並用一句說得出真正原因的話——開到上限時 DSM 回 18990541，到操作者眼前是「配置失敗」，而 `pvesm status` 還顯示好幾 TB 可用。剩下十六個時會警告一次。這個數量包含不屬於這個 storage 的 LUN，而這是它從不送那個會藏起 Virtual Machine Manager 磁碟的型態過濾條件的第二個理由。
- 節點端模組已對硬體驗證：一顆 LUN 掛到 Proxmox VE 節點、裝置由核心的 WWID 確認（推導規則的**第三個**獨立確認）、擴充、然後卸離——並確認節點回到完全相同的先前狀態。

### 修正

- **`make check-zh` 在 62 個真實錯誤上都回報通過**。它沒有 `use utf8;`，所以字元類別裡的 。和 、是位元組，而輸入是已解碼的字元。規則 1 之所以照樣有效，是因為它的範圍寫成 `\x{...}` 轉義——正是這種部分成功讓一個壞掉的守衛看起來像好的。62 處全部修正。
- 同一個守衛接著開始**製造**錯誤：刪掉程式碼區塊會把兩側文字接在一起，產生一個原始檔裡沒有的「全形標點後空格」。程式碼區塊改為取代，不是刪除。
- 守衛現在也看得穿強調標記：「程式碼。**下面」渲染出來的空格，和「程式碼。下面」一模一樣。
- `docs/TESTING.md` 還寫著沒做過寫入測試、也沒有任何 plugin 程式碼。兩件都已經不成立了。
- 兩份 README 現在都連到文件網站，中文版連到它的中文版本——語言在 URL 上，所以連結可以帶著它。

## [0.2.2~beta1] - 2026-08-06

### 修正

- **每一份繁體中文文件在句子中間都出現了多餘的空格**。GitHub 會把兩個中文字之間的換行變成一個**看得見的空格**，所以一段整齊折行的段落到讀者眼前變成「驗證紀錄值得 先讀一遍」。這是看渲染後的頁面才發現的——在編輯器裡完全看不出來。四份中文文件都已取消折行，而 `make check-zh` 現在會在以下情況失敗：中文段落被折行、`*text*` 斜體強調（那是英文排版；中文用粗體或「」）、網站上中文區塊裡的 `<em>`，以及全形標點後面多出的空格。
- 狀態說明還寫著「規格與探索階段」，而且只列了三個模組，實際上有六個。現在它寫的是真的：模組層可以用了，PVE plugin 還沒寫。

### 新增

- `Synology::Command`——有界的外部命令、有界的檔案測試、有界的 sysfs，**幾乎原封不動地從 Dell EMC 專案移植**。這是這個家族裡被硬體打磨最多次的程式碼，重寫等於把塑造它的那些事故丟掉；註解也因此保持原文。
- `Synology::Multipath`——必要的 `conf.d` drop-in（multipathd 沒有內建 Synology 條目，少了它就套用通用預設值，而那些包含 `no_path_retry "queue"`）、以 `dm-uuid-mpath-<wwid>` 定址而不是在 `user_friendly_names yes` 的節點上並不存在的 `/dev/mapper/<wwid>`、一次只動一個 map 的操作，以及因為 `mapping_index` 會重用而改由核心提供的裝置身分。

## [0.2.1~beta1] - 2026-08-06

### 新增

- `Synology::Naming`——名稱建構與**所有權閘門**，也就是決定這個 plugin 可以刪除什麼的那道判斷。它收 storage id，而不只是「看起來像某個 PVE plugin 的名稱」：前置字串識別的是 **storage**，絕不是物件的種類，所以兩半都要檢查。
- 一個讓事情變簡單的實測結果：**快照名稱完全不需要折疊**。DSM 會原封不動地儲存快照名稱裡的底線、空格、`+`、`@` 與開頭數字，所以 PVE 的名稱可以直接通過——而同一顆 LUN 內的重複名稱會被 **18990513** 拒絕，所以名稱可以識別一個快照。
- 多 48 個測試，大多是惡意案例：別人的 LUN、Virtual Machine Manager 的磁碟、前置字串不在開頭、以及帶結尾換行的名稱，全都正確地**不**被視為擁有。

### 修正

- 命名函式現在會拒絕**方法**呼叫，而不是給出錯誤答案。以 `Naming->is_pve_managed_volume(...)` 呼叫時參數會往後位移，於是閘門對一個確實屬於自己的物件回答「不屬於」——安全，但無聲，而列表會空著回來、沒有任何東西解釋原因。這是寫測試時發現的。

## [0.2.0~beta1] - 2026-08-06

**第一批會和 NAS 對話的程式碼**。三個模組、91 個單元測試，以及一次對著 DS918+（DSM 7.1.1）跑完的完整生命週期：建立、重複名稱拒絕、快照、所有權過濾、越過較新快照的倒回、複製、擴充、建立 target、加入式對應、精準解除對應、刪除。

還沒有 PVE plugin——`synologyiscsi` 還不能加到節點上。這是它底下那一層。

### 新增

- `Synology::API`——傳輸層。從 NAS 探索路徑與版本而不是寫死；工作階段**同時**用 cookie 與表單參數攜帶，因為兩份參考實作對哪一種可用的看法不同；回送兩份都不送的防 CSRF token；對要求的 API 做 JSON 參數編碼；失敗時輪替 portal，而 URL 在輪替 **之後**才組；並且**在憑證被拒絕時鎖定、永不重試**——DSM 三次失敗就封鎖一個位址一天，而 PVE 每十秒輪詢一次。
- `Synology::LUN`——LUN 與快照。絕不送那個會藏起 LUN 的型態過濾條件；強制執行 API 不強制的 1 GB 最小值；在本地拒絕不合法的名稱，而不是丟給一台有時會照樣建立的 NAS；**任何建立失敗之後都按名稱查一次**並清掉找到的東西；有界地等待 `is_action_locked`，並把逾時回報成「還在進行」而不是「失敗」；用 `taken_by` 過濾快照，所以使用者自己的快照絕不會被碰；倒回後驗證 LUN 的 uuid 沒有改變。
- `Synology::Target`——target 與對應。每個 target 都以 `max_sessions = 0` 建立，因為預設的 1 只允許一個節點登入；從 NAS 讀取 target 的 IQN，而不是用目前的主機名稱推導；並且把 `mapping_index` 只當成找裝置的線索、絕不當成身分，因為它會被重用。

### 修正

- **一個失敗的 id 查詢被回報成「不存在」。**`target_id` 必須以 JSON 字串送給 DSM——裸的數字會被 **18990710** 拒絕——而查詢的後備路徑只用名稱搜尋，所以用 id 查詢失敗時會落到「找不到」，於是每一次對應檢查都默默回答否。編碼與後備路徑都已修正，兩半都有回歸測試。這正是本專案要避免的「問不到」與「答案是否」的混淆，而它是靠把程式碼拿去對硬體跑、而不是靠閱讀，在自己的程式碼裡找到的。
- `Synology::LUN` 使用了 `JSON::encode_json` 卻沒有自己 import JSON，只是剛好因為 `API.pm` 載入過它才能運作。

## [0.1.4~beta1] - 2026-08-06

階段 4 剩下的寫入測試，包含第一次掛到 Proxmox VE 節點。事後已確認節點與 NAS 都回到完全相同的起始狀態。

### 新增

- **WWID 推導在第二個獨立樣本上得到確認**。一顆掛到節點的 LUN，`scsi_id` 給出的正是預測值。
- 裝置對自己的描述，而這些 NAS 的 API 都不提供：vendor `SYNOLOGY`、product `Storage`（以空白填滿至 16 位元組）、revision `4.0`，以及 **`TPGS=1`**——它宣告支援隱式 ALUA，這就是 multipath 自己啟用 `hwhandler='1 alua'` 的原因。

### 變更

- **裝置不再以 `/dev/mapper/<wwid>` 定址**。測試節點設了 `user_friendly_names yes`，所以 multipath 把 map 命名為 `mpathc`，那個路徑並不存在。而 `/dev/disk/by-id/dm-uuid-mpath-<wwid>` 一直都存在。把 `user_friendly_names no` 設成全域會把其他廠商的 map 一起改名，所以那不是選項。
- **容量絕不由加總 `allocated_size` 得出**。複製一顆內含 512 MiB 的 LUN，複本回報 512 MiB 已配置，卻消耗儲存空間**零**位元組——它是 reflink，共用區塊對兩顆 LUN 都算了一次。一個範本帶二十個連結複本，看起來會像消耗了二十倍。

### 學到的事

- **multipath 沒有任何內建的 Synology 設定**——`multipathd show config` 裡沒有 `SYNOLOGY` 條目——所以 `conf.d` drop-in 是必要的，不是調校。少了它就套用通用預設值，而測試節點上那些預設值包含 `no_path_retry "queue"`。
- `multipath -f` 可能回答「device not found」，因為 `fail_if_no_path` 已經把 map 移除了。那是成功。
- 空間是**延遲**回收的：刪除一顆寫入過 512 MiB 的 LUN 之後，過了幾分鐘儲存空間的可用容量仍然沒變。
- 複製一顆有資料的 LUN 會鎖 3.5 秒；快照不論內容多少都是 0.20 秒。
- 快照的所有權可以過濾：`taken_by` 原封不動回傳，而空值會變成 `webapi`，所以不是本 plugin 拍的快照永遠可以區分。
- **R-14 無法安全地再往下釘**。一個新建的非管理員帳號在登入就被 402 拒絕，還沒機會嘗試任何 SAN API，而每多試一次就用掉自動封鎖那三次失敗額度之一（封鎖一天）。順帶留下兩個發現：`create` 帶 `expired=now` 會默默產生一個無法登入的帳號，而 `set` 帶 `expired=never` 回報成功卻什麼都沒改。

### 修正

- **文件網站進入時預設英文，並把語言帶在 URL 上**（`?lang=zh`）。它原本會參考瀏覽器的地區設定，所以 zh-TW 的瀏覽器會落在一個正式版本是英文的頁面的中文版上。語言在 `<head>` 就套用，所以不會閃到錯誤的語言；用 `pushState`，所以上一頁會回到讀者原本的語言。

## [0.1.3~beta1] - 2026-08-06

在一台 DS918+（DSM 7.1.1）上執行階段 4 的寫入測試，使用專用的 `pvetest-` 前置字串，建立的每個物件事後都已刪除，並確認陣列回到原本的內容。

### 新增

- **支援倒回。**`restore_snapshot(src_lun_uuid, snapshot_uuid)` 不會改變 LUN 的 uuid——所以 WWID 存活——而且比還原點更新的快照會保留。因此 `volume_rollback_is_possible` **不需要**相關專案那道拒絕，一顆磁碟也可以反覆倒回。
- **兩種高可用性架構都支援**：Synology HA（一個浮動叢集 IP，像 Pure 的 `vir0`）與 UC／SA 雙控制器（每個控制器一個位址，透過 `relay_node` 探索）。兩種都沒有在實機上跑過；plugin 偵測到 `DSM UC` 時發出警告而不是拒絕，而文件在有人回報之前一律寫「未驗證」。
- 從快照複製出來的是**精簡的**（`allocated_size: 0`），所以連結複本與範本是真的便宜。

### 修正／學到的事

- **回報失敗的建立仍可能把 LUN 建出來**——255 字元的名稱被 18990068 拒絕，而 LUN 照樣被建立。失敗的建立絕不被相信：事後按名稱查一次，找到的就接管或刪掉。
- **`mapping_index` 會重用**。被釋放的編號會給下一顆被對應的 LUN，所以殘留的裝置路徑會指向另一顆磁碟。裝置身分來自核心的 WWID，絕不來自路徑——而兩份公開參考實作正是只依賴路徑。
- **沒有任何東西因為依賴關係而拒絕刪除**——有快照的 LUN、有存活複本的快照、已**對應**的 LUN，都不拒絕。所以「刪除前先解除對應」完全是這個 plugin 的責任。
- `map_target` 是加入，`unmap_target` 只移除指定的那些，與 Unity 取代整份清單的行為正好相反。
- 十六個同時發出的建立全部成功，而陣列與 API 回報的一致；第二次登入不會擠掉第一次。
- `_` 不是合法的 LUN 名稱字元，空格、`+`、`@` 也不是。容量會精確建立、完全不進位——而文件寫的 1 GB 最小值並未被 API 強制，所以由 plugin 自己守。

## [0.1.2~beta1] - 2026-08-06

### 修正

- **發佈的 `SHA256SUMS` 無法驗證**。GitHub 會把 release 附件檔名裡的 `~` 改寫成 `.`，所以檢查碼檔案裡寫的是 `..._0.1.1~beta1-1_all.deb`，而實際提供下載的檔案叫 `..._0.1.1.beta1-1_all.deb`，`sha256sum -c SHA256SUMS` 會回答「找不到檔案」。release 工作流程現在會先把套件改名成 GitHub 實際會提供的名稱再計算雜湊，然後**驗證自己產生的檢查碼檔案**，而不是假設它對。`.deb` 本身一直是正確的——dpkg 從套件的 control 資料讀版本，從不看檔名——所以 0.1.0~beta1 與 0.1.1~beta1 都能正常安裝，只有它們的檢查碼檔案不能用。

### 新增

- 這份變更記錄，中英文各一份。`make release-check` 現在會要求發行版本必須有對應的條目。

## [0.1.1~beta1] - 2026-08-06

### 新增

- **快照還原的方法名稱是 `restore_snapshot`**，在實機上確認：九個候選名稱送進一台 DSM 7.1.1，每個都指名一個 NAS 從未發出過的 LUN 與快照 uuid，其中一個回答了 `18990505 bad LUN UUID`——證明它存在，而且已經走到查詢 LUN 那一步。兩份公開的參考實作都沒有這個方法。
- 記錄錯誤碼 18990004，從 `SYNO.Core.ISCSI.Host` 的 `get` 觀察到。

### 變更

- **LUN 列表不再送型態過濾條件**。Synology 自己的 CSI driver 會送一個明確的十二種型態清單，而在測試機上那份清單藏了一個 Virtual Machine Manager 的虛擬磁碟，它的 120 GiB 吃的是同一個儲存空間。一個已被驗證為不完整的過濾條件比不過濾更糟，因為靜默少掉物件的清單，會被「決定什麼可以刪」的程式碼讀成「全部就這些」。探索工具現在會跑兩次列表，並回報過濾條件藏了什麼。
- **倒回仍然拒絕**。知道一個方法存在，不等於知道它做什麼：它的參數名稱尚未確認，倒回後比還原點更新的快照是否保留、LUN uuid 是否不變，也都還沒確認。

### 修正

- 用 `--no-luns` 跳過列表時，驗證紀錄會回報成「這個帳號**無法**讀取」。跳過、被拒絕、成功是三種不同的結果，而把前兩者混為一談正是這個專案的規則要防止的事。
- 唯讀方法成功時被標成可疑。只有破壞性候選方法成功才值得警戒。
- 檔頭註解還在描述 `--probe-methods` 送出「不帶參數」的呼叫，而改成指名一個不存在的 uuid 之後那句話已經不成立。

## [0.1.0~beta1] - 2026-08-06

第一個打包版本。**不含任何 storage plugin。**

### 新增

- `pve-syno-api-probe`——以唯讀方式探索 Synology DSM 的 SAN Web API。它探索 API 路徑與版本範圍而不是寫死，工作階段兩種載體都送（因為兩份公開參考實作對哪一種可用的看法不同），回送兩份參考實作都不送的防 CSRF token，而且絕不把憑證放進 URL。
- `docs/TESTING_zh-TW.md`——哪些事實已在實機驗證、哪些沒有，以及補上落差的測試計畫。
- `docs/DSM-ACCOUNT_zh-TW.md`——plugin 需要的 DSM 帳號、可以從它拿掉什麼，以及讓「重試被拒絕的憑證」變得危險的自動封鎖行為。
- 建置守衛：不得有節點層級的 `multipath` flush，憑證不得出現在 URL。
